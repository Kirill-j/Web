extends ../head

block content
    h2= title
    a(href="/addDiscipline") Добавить дисциплину
    ul
        each el in disciplines
            li
                a(href="/discipline/" + el.id)= el.name