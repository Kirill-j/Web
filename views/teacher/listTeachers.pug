extends ../head

block content
    a(href="/addTeacher" class="btn btn-success") Добавить преподавателя
    ul(class="list-group")
        each el in teachers
            li(class="list-group-item")
                a(href="/teacher/" + el.id)= el.name